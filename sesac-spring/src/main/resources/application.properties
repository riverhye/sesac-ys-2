spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/sesac?useUnicode=yes&characterEncoding=UTF-8&allowMultiQueries=true&serverTimezone=Asia/Seoul
spring.datasource.username=root
spring.datasource.password=name123$

mybatis.type-aliases-package=com.spring.boot.mapper
mybatis.mapper-locations=mybatis-mapper/*.xml

# check sql - ?? SQL? ??
spring.jpa.show-sql=true

spring.jpa.hibernate.ddl-auto=update
# create : drop table and create (DROP + CREATE)
# create-drop : Add create attribute that drop table when the application ends (DROP + CREATE + DROP)
# update : compare DB table with Entity information and alternate it (ALTER TABLE)
# validate : compare DB table and Entity information and if there's any difference, don't run
# none : not use automatically create function